# Copy this file to env/mailserver.env on the VPS and replace every example value.

MAIL_DOMAIN=example.com
MAIL_HOSTNAME=mail.example.com
LETSENCRYPT_EMAIL=admin@example.com
POSTMASTER_ADDRESS=postmaster@example.com

# Optional while testing certificate issuance. Remove or leave empty for production.
# CERTBOT_EXTRA_ARGS=--staging
